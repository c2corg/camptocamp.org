fields:
  password:
    group:            password_group
  new_password:
    group:            password_group
    sfCompareValidator:
      check:          password
      compare_error:  The two passwords do not match      
  
  email:
    required:       yes
      msg:          You must specify an email
    sfEmailValidator:
      strict:       true
      email_error:  This email address is invalid
    myUniqueValidator:
      class:        UserPrivateData
      column:       email
      unique_error: This email already exists. Please choose another one.
  
  edit_nickname:
    required:       yes
      msg:          You must specify a nickname
    sfDoctrineUniqueValidator:
      class:        UserPrivateData
      column:       username
      unique_error: This nickname already exists. Please choose another one.
  
  edit_topo_name:
    required:       yes
      msg:          You must specify a full name
    sfStringValidator:
      min:          3
      min_error:    this name is too short (3 characters minimum)
